<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <link href="https://unpkg.com/tailwindcss@^2/dist/tailwind.min.css" rel="stylesheet">
    <title>Register - FinTracker</title>
</head>

<body>
    <div class="min-h-screen bg-gray-100 text-gray-900 flex justify-center">
        <div class="max-w-screen-xl m-0 sm:m-10 bg-white shadow sm:rounded-lg flex justify-center flex-1">
            <div class="lg:w-1/2 xl:w-5/12 p-6 sm:p-12">
                <div>
                    <h1>Join FinTracker</h1>
                </div>
                <div class="mt-12 flex flex-col items-center">
                    <div class="w-full flex-1 mt-8">
                        <div class="flex flex-col items-center">
                            <a href="/login" class="w-full max-w-xs font-bold shadow-sm rounded-lg py-3 bg-green-100 text-gray-800 flex items-center justify-center transition-all duration-300 ease-in-out focus:outline-none hover:shadow focus:shadow-sm focus:shadow-outline">
                                <span>Login</span>
                            </a>
                            
                            <!-- Display errors -->
                            <% if (error && error.length > 0) { %>
                                <div class="mt-4 p-3 bg-red-100 border border-red-400 text-red-700 rounded">
                                    <%= error %>
                                </div>
                            <% } %>
                            
                            <!-- Display success message -->
                            <% if (message && message.length > 0) { %>
                                <div class="mt-4 p-3 bg-green-100 border border-green-400 text-green-700 rounded">
                                    <%= message %>
                                </div>
                            <% } %>

                        </div>

                        <div class="my-12 border-b text-center">
                            <div
                                class="leading-none px-2 inline-block text-sm text-gray-600 tracking-wide font-medium bg-white transform translate-y-1/2">
                                Or create account with your email
                            </div>
                        </div>
                        <form action="/register" method="POST">

                            <div class="mx-auto max-w-xs">
                                <input
                                    class="w-full px-8 py-4 rounded-lg font-medium bg-gray-100 border border-gray-200 placeholder-gray-500 text-sm focus:outline-none focus:border-gray-400 focus:bg-white"
                                    type="text" id="fullname" name="fullname" value="<%= fullname %>" placeholder="Full Name" required />
                                <input
                                    class="w-full px-8 py-4 rounded-lg font-medium bg-gray-100 border border-gray-200 placeholder-gray-500 text-sm focus:outline-none focus:border-gray-400 focus:bg-white mt-5"
                                    type="email" id="email" name="email" value="<%= email %>" placeholder="Email" required />
                                <input
                                    class="w-full px-8 py-4 rounded-lg font-medium bg-gray-100 border border-gray-200 placeholder-gray-500 text-sm focus:outline-none focus:border-gray-400 focus:bg-white mt-5"
                                    type="password" id="password" name="password" placeholder="Password" required />
                                <div class="flex mt-5 space-x-2">
                                    <select id="sexe" name="sexe" required
                                        class="w-1/2 px-8 py-4 rounded-lg font-medium bg-gray-100 border border-gray-200 placeholder-gray-500 text-sm focus:outline-none focus:border-gray-400 focus:bg-white">
                                        <option value="">Gender</option>
                                        <option value="male" <%= sexe === 'male' ? 'selected' : '' %>>Male</option>
                                        <option value="female" <%= sexe === 'female' ? 'selected' : '' %>>Female</option>
                                        <option value="other" <%= sexe === 'other' ? 'selected' : '' %>>Other</option>
                                    </select>
                                    <input type="number" id="age" name="age" value="<%= age %>" min="18" max="120" required
                                        class="w-1/2 px-8 py-4 rounded-lg font-medium bg-gray-100 border border-gray-200 placeholder-gray-500 text-sm focus:outline-none focus:border-gray-400 focus:bg-white"
                                        placeholder="Age">
                                </div>
                                <button type="submit"
                                    class="mt-5 tracking-wide font-semibold bg-green-500 text-white w-full py-4 rounded-lg hover:bg-green-600 transition-all duration-300 ease-in-out flex items-center justify-center focus:shadow-outline focus:outline-none">
                                    <svg class="w-6 h-6 -ml-2" fill="none" stroke="currentColor" stroke-width="2"
                                        stroke-linecap="round" stroke-linejoin="round">
                                        <path d="M16 21v-2a4 4 0 00-4-4H5a4 4 0 00-4 4v2" />
                                        <circle cx="8.5" cy="7" r="4" />
                                        <path d="M20 8v6M23 11h-6" />
                                    </svg>
                                    <span class="text-white">
                                        Create Account
                                    </span>
                                </button>
                                <p class="mt-6 text-xs text-gray-600 text-center">
                                    By creating an account, you agree to our
                                    <a href="#" class="border-b border-gray-500 border-dotted">
                                        Terms of Service
                                    </a>
                                    and
                                    <a href="#" class="border-b border-gray-500 border-dotted">
                                        Privacy Policy
                                    </a>
                                </p>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
            <div class="flex-1 bg-gradient-to-br from-green-400 to-blue-600 text-center hidden lg:flex items-center justify-center">
                <div class="text-white p-8">
                    <img src="/img/modern-financial-dashboard-interface-on-mobile-pho.jpg" alt="Financial Dashboard" class="w-80 h-auto rounded-lg shadow-2xl mb-6 mx-auto">
                    <h3 class="text-2xl font-bold mb-4">Start Your Financial Journey</h3>
                    <p class="text-lg opacity-90">Join thousands of users managing their finances smarter with FinTracker</p>
                </div>
            </div>
        </div>
    </div>
</body>

</html>
